# cargo-mutants configuration.
#
# The list below names mutants that were individually triaged and VERIFIED
# EQUIVALENT: each was applied by hand and survived the full test suite plus
# large differential query corpora, with a written justification (see the
# mutation-triage test files under tests/ and the PR history for details).
# Typical reasons: error-path guards where both variants produce the same
# parse error, min/max ties, and states unreachable by construction.
#
# The generated LALRPOP module is kept out of the run by the
# `#[cfg_attr(mutants, mutants::skip)]` marker on `mod generated;` in
# src/parser.rs (plus `--exclude 'src/generated/**'` in mutation.yml as a
# fallback). cargo-mutants 27.x walks every module for mutants *before*
# applying path excludes, and walking the 5 MB generated file costs minutes
# per invocation (391 s per CI shard); the skip marker stops it from loading
# the file at all, so `cargo mutants --list` finishes in well under a second.
#
# MAINTENANCE: entries pin exact file:line:col positions, so they go stale
# when the surrounding code shifts. After editing src/parser.rs (or the other
# listed files), re-run `cargo mutants --list` and refresh the line numbers;
# a stale entry simply stops matching (the mutant reappears as MISSED) - it
# never hides a new mutant.

exclude_re = [
    'src/diagnostic\.rs:478:18: replace > with >= in floor_char_boundary',
    'src/parser\.rs:1863:40: replace && with \|\| in parameter',
    'src/parser\.rs:524:19: replace == with != in normalize_diagnostics',
    'src/parser\.rs:526:29: replace == with != in normalize_diagnostics',
    'src/parser\.rs:93:26: replace \|\| with && in parse_recovering',
    'src/parser\.rs:93:29: delete ! in parse_recovering',
    'src/span\.rs:78:27: replace < with <= in Span::cover',
    'src/span\.rs:83:25: replace > with >= in Span::cover',
    'src/parser\.rs:2074:13: delete match arm TokenKind::LeftParen in top_level_pipe_candidates',
    'src/parser\.rs:2074:49: replace \+= with \*= in top_level_pipe_candidates',
    'src/parser\.rs:2076:13: delete match arm TokenKind::LeftBrace in top_level_pipe_candidates',
    'src/parser\.rs:2076:44: replace \+= with \*= in top_level_pipe_candidates',
    'src/parser\.rs:2078:32: replace match guard brackets == 1 && parentheses == 0 && braces == 0 with true in top_level_pipe_candidates',
    'src/parser\.rs:2078:46: replace && with \|\| in top_level_pipe_candidates',
    'src/parser\.rs:2078:66: replace && with \|\| in top_level_pipe_candidates',
    'src/parser\.rs:2095:44: replace && with \|\| in parse_comprehension_tokens',
    'src/parser\.rs:2099:43: replace && with \|\| in parse_comprehension_tokens',
    'src/parser\.rs:2106:36: replace \+ with - in parse_comprehension_tokens',
    'src/parser\.rs:2106:36: replace \+ with \* in parse_comprehension_tokens',
    'src/parser\.rs:2106:40: replace < with <= in parse_comprehension_tokens',
    'src/parser\.rs:2125:37: replace \|\| with && in parse_comprehension_tokens',
    'src/parser\.rs:2125:45: replace \+ with - in parse_comprehension_tokens',
    'src/parser\.rs:2125:45: replace \+ with \* in parse_comprehension_tokens',
    'src/parser\.rs:2130:24: replace match guard index \+ 1 < pipe with true in parse_comprehension_tokens',
    'src/parser\.rs:2130:30: replace \+ with - in parse_comprehension_tokens',
    'src/parser\.rs:2130:30: replace \+ with \* in parse_comprehension_tokens',
    'src/parser\.rs:2130:34: replace < with <= in parse_comprehension_tokens',
    'src/parser\.rs:2334:37: replace < with <= in nested_parser_tokens',
    'src/parser\.rs:2433:13: delete match arm ParserToken::LeftBrace in find_top_level_token',
    'src/parser\.rs:2433:46: replace \+= with \*= in find_top_level_token',
    'src/parser\.rs:1148:19: replace > with >= in relationship_label_end',
    'src/parser\.rs:1152:13: replace \|\| with && in relationship_label_end',
    'src/parser\.rs:1211:39: replace match guard nested_brackets != 0 with true in is_label_predicate_context',
    'src/parser\.rs:1219:37: replace match guard nested_braces != 0 with true in is_label_predicate_context',
    'src/parser\.rs:137:73: replace - with / in parse_tokens_with_contextual_names',
    'src/parser\.rs:178:44: replace < with <= in parse_tokens_with_contextual_names',
    'src/parser\.rs:2240:18: replace > with < in parse_label_expression',
    'src/parser\.rs:2280:17: replace \|\| with && in legacy_label_tokens_are_consistent',
    'src/parser\.rs:979:39: replace match guard nested_brackets != 0 with true in is_pattern_expression_context',
    'src/parser\.rs:987:37: replace match guard nested_braces != 0 with true in is_pattern_expression_context',
]
